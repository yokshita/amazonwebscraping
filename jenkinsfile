pipeline {
    agent any

    stages {
        stage('test using marker') {
            steps {
                bat 'pytest -m 'tc_1''
                bat 'pytest -m 'tc_2''
            }
        }
    }
}
